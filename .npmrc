# .npmrc - pnpm configuration for Azure App Service

# Automatically installs peer dependencies without prompting
auto-install-peers=true

# Use hoisted node_modules structure for Azure compatibility
# Azure App Service expects a flattened dependency tree
node-linker=hoisted
